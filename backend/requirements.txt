# finance_client installed from /deps/finance_client in Docker (see Dockerfile)
# For local dev outside Docker: pip install -e ../../finance_client

# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart

# Database
sqlalchemy[asyncio]>=2.0
asyncpg
psycopg2-binary
alembic

# Task queue
celery[redis]>=5.3
redis[hiredis]

# Auth
passlib[bcrypt]

# MCP server
fastmcp>=0.4

# Google AI (LLM agent handler)
google-genai
google-adk

# Browser automation (web report collector, replaces Puppeteer/Selenium)
playwright

# Data / ML
numpy
pandas
torch
scikit-learn
matplotlib
yfinance
pandas_datareader

# finance_client transitive deps (installed --no-deps so we list them explicitly)
websocket-client
PyYAML
google-auth-oauthlib

# Scheduling
apscheduler>=3.10

# Utilities
pydantic-settings
python-dotenv
httpx
orjson
statsmodels
scipy
pyarrow
